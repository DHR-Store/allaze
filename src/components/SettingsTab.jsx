import React from 'react';

function SettingsTab() {
  return (
    <div>
      <h5>API Configuration</h5>
      <p>All data is retrieved from public APIs (myvariant.info, UniProt, Swiss‑Model).</p>
      <p>No local database required.</p>
    </div>
  );
}

export default SettingsTab;